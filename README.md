# cloudflare-ai-youxuan

Cloudflare Workers 项目，入口文件为 `coming-soon.js`。

## 启动命令

```bash
npm install
npm start
```

或使用开发命令：

```bash
npm run dev
```

默认会通过 Wrangler 在本地启动 Worker（通常为 `http://localhost:8787`）。

## 部署命令

```bash
npm run deploy
```